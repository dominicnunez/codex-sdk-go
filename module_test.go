package codex

import (
	"testing"
)

// TestModuleSetup verifies the module is correctly initialized.
// This test ensures the package compiles and the module path is accessible.
func TestModuleSetup(t *testing.T) {
	// If this test compiles and runs, the module is correctly set up.
	t.Log("Module github.com/dominicnunez/codex-sdk-go initialized successfully")
}
